# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
.env

# Tooling
.mypy_cache/
.pytest_cache/
.ruff_cache/
.coverage
htmlcov/

# IDE
.vscode/
.idea/
*.swp
.DS_Store

# Secrets / local config (never commit)
*.pem
*.key
secrets/
local.settings.*

# Data / models (never commit large artifacts)
data/
*.ckpt
*.pt
*.bin

# Olist source tables and the per-entity CSVs derived from them (~74MB).
# Reproduce with: python examples/build_supply_data.py --download
examples/olist_raw/
# Step 1 output, ~31MB. What the repository carries is the step 2 cut,
# examples/supply_sample, which is committed.
examples/supply_full/
